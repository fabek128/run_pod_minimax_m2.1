services:
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    ports:
      - "3000:8080"
    environment:
      OPENAI_API_BASE_URL: "${OPENAI_API_BASE_URL:-http://localhost:8000/v1}"
      OPENAI_API_KEY: "${OPENAI_API_KEY:-local-key}"
      DEFAULT_MODELS: "${DEFAULT_MODELS:-local-gguf}"
    volumes:
      - openwebui-data:/app/backend/data

volumes:
  openwebui-data:
